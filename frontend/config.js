const config = {
    apiBaseUrl: process.env.NEXT_PUBLIC_API_BASE_URL || "http://localhost:5001",
    appName: "Expert Solvers",
  
    // static values
    name: "Loan",
    subname: "Property"
  };
  
  export default config;